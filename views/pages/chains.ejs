    <%- include('../partials/headbs5'); %>

    <%- include('../partials/headerbs5'); %>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <script>
      const myArray = [
        { name: "Bitcoin",
          address: "12KYrjTdVGjFMtaxERSk3gphreJ5US8aUP" },
        { name: "Cardano",
          address: "addr1wysmmrpwphe0h6fpxlmcmw46frmzxz89yvpsf8cdv29kcnqsw3vw6" },
        { name: "Litecoin",
          address: "LVg2kJoFNg45Nbpy53h7Fe1wKyeXVRhMH9" }
      ];

      
    </script>

    <div class="overflow-y-auto align-items-start"> 
   

    <div class="container-fluid">
         <!-- Top Row -->
         <div class="row align-items-center border-top px-3 pt-2">
          <!-- Left Column -->
          <div class="col text-start">
            <p>Cardano</p>  
          </div>
          <!-- Right Column -->
        
          <div class="col text-end">
            <button type="button" class="btn mt-0 me-0" title="Copy to clipboard" onclick="copyAddress()">
              <svg class="bi" aria-hidden="true"><use xlink:href="../js/copy.svg#copy"/></svg>
                
            </button>
            
          </div>
        </div>
        <!-- Bottom Row -->
        <div class="row mt-0 mb-3 border-bottom ps-3 overflow-x-auto">
          <div class="col">
            <p id="myInput">addr1wysmmrpwphe0h6fpxlmcmw46frmzxz89yvpsf8cdv29kcnqsw3vw6</p>
            
          </div>
        </div>
      </div>

      <div id="yourTargetDiv" ></div>

      <script>
        
        function copyAddress(index) {
          // Get the paragraph element
          
          let address = document.getElementById("myInput"+index);
          
          // Copy the text inside the paragraph to the clipboard
          navigator.clipboard.writeText(address.textContent).then(() => {
            alert("Address copied to clipboard: " + address.textContent);
            
          }).catch(err => {
            console.error("Failed to copy text: ", err);
          });
        }
      </script>
       
    
<script> 

function dynamicRow(address, name, index) {
// Create container-fluid div
const myInput = "myInput"+index;

const container = document.createElement("div");
container.classList.add("container-fluid");

// Create top row
const topRow = document.createElement("div");
topRow.classList.add("row", "align-items-center", "border-top", "px-3", "pt-2");

// Create left column
const leftCol = document.createElement("div");
leftCol.classList.add("col", "text-start");
const leftText = document.createElement("p");
leftText.textContent = name;
leftCol.appendChild(leftText);

// Create right column
const rightCol = document.createElement("div");
rightCol.classList.add("col", "text-end");
const button = document.createElement("button");
button.setAttribute("type", "button");
button.classList.add("btn", "mt-0", "me-0");
button.setAttribute("title", "Copy to clipboard");
//button.setAttribute("onclick", "copyAddress()");
//button.addEventListener("click", copyAddress);
button.addEventListener("click", function(){ copyAddress(index); });
//button.innerHTML = '<i class="bi bi-copy"></i>';
const i = document.createElement("i");
i.classList.add("bi","bi-copy");
button.appendChild(i);
//Create SVG element
// const svg = document.createElement("svg");

// svg.classList.add("bi", "border");
// svg.setAttribute("aria-hidden", "true");
// //svg.setAttribute("width", "40");
// //svg.setAttribute("height", "40");
// const use = document.createElement("use");
// use.setAttribute("xlink:href", "/js/copy.svg#copy");

// // use.setAttribute("width", "40");
// // use.setAttribute("height", "40");
// svg.appendChild(use);

// // Append SVG to button, button to right column
// button.appendChild(svg);

rightCol.appendChild(button);

// Append left and right columns to top row
topRow.appendChild(leftCol);
topRow.appendChild(rightCol);

// Create bottom row
const bottomRow = document.createElement("div");
bottomRow.classList.add("row", "mt-0", "mb-3", "border-bottom", "ps-3", "overflow-x-auto");
const bottomCol = document.createElement("div");
bottomCol.classList.add("col");
const bottomText = document.createElement("p");
bottomText.setAttribute("id", myInput);
bottomText.textContent = address;

// Append text to column, column to bottom row
bottomCol.appendChild(bottomText);
bottomRow.appendChild(bottomCol);

// Append rows to container
container.appendChild(topRow);
container.appendChild(bottomRow);

// Append container to body or a specific element
document.getElementById("yourTargetDiv").appendChild(container);

//alert(use.getAttribute("xlink:href")); 
 
    }
    

      myArray.forEach((element, index, array) => { 
        dynamicRow(element.address,element.name, index);
        console.log(element.name + "\n"+element.address); // 100, 200, 300
        console.log(index); // 0, 1, 2
        //console.log(array); // same myArray object 3 times
        
      });

  </script>

    </div>


    <%- include('../partials/footer'); %>
